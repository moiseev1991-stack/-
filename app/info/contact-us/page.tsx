import { createPolisePageExports } from "@/lib/polise/polise-page-bundle";

const { Page, generateMetadata } = createPolisePageExports("اتصل-بنا", "/اتصل-بنا");
export { generateMetadata };
export default Page;
